<div class="client">
  <h2><%= client_name(client) %></h2>
  <p><%= client_email(client) %></p>
  <p><%= client_phone(client) %></p>
  <%= link_to "Edit", edit_client_path(client) %>, <%= link_to "Delete", client, method: :delete, data: { confirm: "Are you sure you want to delete this client?" } %>   
</div>